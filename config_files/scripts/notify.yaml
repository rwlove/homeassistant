notify_kodi_frontends:
  sequence:
  - service: notify.righteye
    data_template:
      title: "Home Assistant"
      message: "{{ message }}"
      data:
        icon: "info"
        displaytime: 10000
  - service: notify.lefteye
    data_template:
      title: "Home Assistant"
      message: "{{ message }}"
      data:
        icon: "info"
        displaytime: 10000

notify_kodi_frontends_new:
  sequence:
  - service: notify.righteye
    data_template:
      title: "Home Assistant"
      message: "This is a test, do not be alarmed!"
      data:
        icon: "info"
        displaytime: 10000