notify_kodi_frontends:
  sequence:
  - service: notify.righteye
    data_template:
      title: "Home Assistant"
      message: "{{ message }}"
      data:
        icon: "info"
        displaytime: 10000
  - service: notify.lefteye
    data_template:
      title: "Home Assistant"
      message: "{{ message }}"
      data:
        icon: "info"
        displaytime: 10000

notify_kodi_frontends_new:
  sequence:
  - service: notify.notify
    data:
      title: "Home Assistant"
      message: "this is still a test, delete me"
      data:
        icon: "info"
        displaytime: 10000