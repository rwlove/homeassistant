- platform: template
  sensors:
    bed_pressure:
      friendly_name: "In Bed"
      value_template: >-
        {% if is_state("binary_sensor.__sensor_10_0", "on") %}
          No
        {% elif is_state("binary_sensor.__sensor_10_0", "off") %}
          Yes
        {% else %}
          Unknown
        {% endif %}